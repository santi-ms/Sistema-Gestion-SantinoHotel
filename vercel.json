{
  "buildCommand": "cd frontend/hotel-frontend && npm install && npm run build",
  "outputDirectory": "frontend/hotel-frontend/dist",
  "installCommand": "cd frontend/hotel-frontend && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

